package com.company;

public class Media extends Items {

    Media(String title, int value, boolean requests, boolean reference, int quantity) {
        super(title, value, requests, reference, quantity, "Media");
    }
}
